import { Button, styled } from '@mui/material'

export const AuthCustomButton = styled(Button)(({ theme }) => ({
    borderRadius: '3.125rem',
    textTransform: 'uppercase',
    maxWidth: '11.875rem',
}))
