import { styled } from '@mui/material/styles'
import { Avatar } from '@mui/material'

export const CustomAvatarMedium = styled(Avatar)(({ theme }) => ({
    height: '2.5rem',
    width: '2.5rem',
}))
